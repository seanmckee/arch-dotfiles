# Keep history local by default. `atuin register` enables encrypted sync later.
style = "compact"
inline_height = 20
show_preview = true
enter_accept = true
keymap_mode = "vim-normal"
filter_mode = "global"
filter_mode_shell_up_key_binding = "directory"
search_mode = "fuzzy"
